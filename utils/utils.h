#include <string>
#include <vector>
#include <sstream>
#include <iterator>

class Utils {

    public:
        static std::vector<std::string> split(const std::string &s, char delim);
    protected:

    private:
};
